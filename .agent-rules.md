
## Environment Bootstrap (CRITICAL)
- Before running ANY build, dev, or test command, ALWAYS run the package manager install first:
  - Node.js projects:  (or  /  if lockfile indicates)
  - Python projects:  or 
- If a command fails with "not found", check if dependencies are installed before escalating
- If , , , or similar CLI tools are not found, run  first
- After installing dependencies, retry the failed command before reporting failure

## Self-Healing Patterns
- Missing dependency → install it → retry
- Build failure → read error output → attempt fix → retry (max 2 attempts)
- Port conflict → try next port (3001, 3002, etc.)
- Permission error → check file ownership, do NOT use sudo
